import countries (country);
import geo (geo as geo-point);

record address (
    float32 lat,
    float32 lng,
    text street,
    text town,
    country country,
    text postal-code,
);


record pin (
    geo-point geo,
);
